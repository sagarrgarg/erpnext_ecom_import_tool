{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,re;d=json.load(sys.stdin);f=d.get('tool_input',{}).get('file_path','') or d.get('tool_response',{}).get('filePath','');exit(0 if f and re.search(r'\\.(py|json|js)$',f) else 1)\" && cd /home/ubuntu/frappe-bench-new && bench --site erpnextkgopl.local migrate 2>&1 | tail -3 || true",
            "timeout": 120,
            "statusMessage": "Running bench migrate..."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(cd /home/ubuntu/frappe-bench-new && bench *)",
      "Bash(ruff *)",
      "Bash(git *)",
      "Bash(find *)",
      "Bash(grep *)",
      "Bash(wc *)",
      "Bash(ls *)",
      "Read",
      "Bash(python3 *)"
    ]
  }
}
